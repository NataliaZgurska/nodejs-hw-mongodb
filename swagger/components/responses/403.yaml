description: This contact does not belong to this user
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 403
        message:
          type: string
          example: This is not your contact!
